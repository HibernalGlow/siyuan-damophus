---
"siyuan-damophus": minor
---

- 数据映射：不再需要手动输入数据库 ID，选中数据库块即可自动标记为索引数据库；也可在任意数据库块右键菜单中直接标记、取消或同步题库统计，新增命令面板入口可在不打开题库的情况下同步全部索引数据库。
- 修复数据映射同步报 "item not found" 的问题：目标数据库中存在残留的无效行时，同步不再中断，会自动重建有效行并可一键清理失效行。
- 数据映射同步速度大幅提升：逐格写入改为整行批量写入，题目较多时同步耗时明显缩短。
