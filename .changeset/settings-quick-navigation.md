---
"siyuan-damophus": minor
---

Group the settings sidebar and module switches into collapsible categories, and add quick switching on both: a module search filter that auto-expands matches, plus expand-all/collapse-all toggles, so the full settings tree fits the initial window without scrolling.
