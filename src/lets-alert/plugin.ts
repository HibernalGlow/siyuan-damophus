import type { PluginMetadata } from "@/types/plugin";

export const pluginMetadata: PluginMetadata = {
  name: "lets-alert",
  displayName: "更新说明",
  description: "接收 Damophus 的更新说明与日志",
  version: "1.0.0",
  enabled: true,
};

export default pluginMetadata;
