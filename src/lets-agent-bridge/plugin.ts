import type { PluginMetadata } from "@/types/plugin";

const pluginMetadata: PluginMetadata = {
  name: "agentBridge",
  displayName: "lets-agent-bridge.displayName",
  description: "lets-agent-bridge.description",
  version: "0.0.4",
  author: "HibernalGlow",
  enabled: true,
  icon: "bot",
};

export default pluginMetadata;
