# 2026-09-07

## 题库插件工作台：桌面/移动 UI 手动切换 + 快速按钮

- 新增 `src/lets-question-bank/workspace/ui-mode.ts`：设备本地 localStorage 偏好（`damophus-question-bank.ui-mode`），三态 `auto | mobile | desktop`，含 `cycleUiMode`。
- `question-bank.css`：三个 `@container (max-width: 960/760/430px)` 块用一次性 Node 脚本机械转换为 `.question-bank:is([data-ui-width="medium|narrow|tiny"])` 属性选择器（JS 实测驱动，容器查询不再使用）。转换规则：选择器若以 `.question-bank` 开头（后跟空格/`[`/`:` 等）则就地插入属性，否则加前缀；合并选择器按逗号逐段处理。标题栏 grid 从 3 列改 4 列（新增布局切换按钮位）。
- `QuestionBankView.svelte`：根元素写 `data-ui-mode` + `data-ui-width`（onMount 同步测宽 + ResizeObserver 跟随；强制模式覆盖测量值：mobile→narrow，desktop→wide）。工作台标题栏新增快速按钮（MonitorSmartphone/Smartphone/Monitor 图标循环 auto→mobile→desktop）。`mobileBreadcrumb` 改传 `effectiveMobileBreadcrumb`（跟随强制模式，auto 时保持 isMobile 原状避免窗口缩放抖动）。
- i18n：parts 文件新增 `uiModeTitle/uiModeAuto/uiModeMobile/uiModeDesktop`（en+zhCN 对称）。
- 验证：svelte-check 通过（仅存量 playlist 相关 2 error 与本次无关）；plugin.test/tab-contract 单测通过；`vite build` 成功。浏览器测试被环境 safe-delete shim 破坏无法启动（vite 清缓存 trash 报错），属沙箱问题。

## 坑点

- 工作目录有并发 git 活动：提交 137f248（题单练习）在我编辑中途把 QuestionBankView 的 import+状态块脚手架一并卷入（该 commit 的 css 无 data-ui-width，转换完整留在工作区）。下次改代码前先确认用户 IDE 是否开着自动暂存/提交。
- 环境里 pnpm 引擎损坏（ERR_PNPM_ENGINE_BIN_MISSING），直接用 `node node_modules/<bin>` 调工具；vitest run 全目录会被 SIGTERM，需按文件指定。
- 首次跑转换脚本后文件被还原过一次（编辑器缓冲区回写），写盘后要立刻在同一条命令里 grep 验证。

## 题单功能「看不到 UI」排查与修复（提交 137f248 之后）

- 现象：题单（chips + 管理对话框）在工作区 dist 与代码里都在，但思源里看不到。
- 根因：思源实际加载的是 `D:\1STUDY\SIYUAN\data\plugins\siyuan-damophus`，副本停留在 9/6 22:48 的旧构建（`grep -c "playlist-chips" index.js` = 0），而 `dist/` 是 9/7 17:39 的新构建。`pnpm build` 只产出 dist，不会安装到思源。
- 修复：`node scripts/deploy-test-workspace.mjs --workspace D:/1STUDY/SIYUAN`（staging + backup + rename，安全可回滚），部署后验证 `grep playlist-chips`、`i18n/zh-CN.json` 含 `lets-question-bank.playlistManage`。仍需重载思源插件才生效。
- 排查口诀：改了代码看不到 → 先比对「思源插件目录 index.js」与「dist/index.js」时间戳 + grep 新特性的 testid/文案。

## 下次这样做

- 批量 CSS 结构转换：先 `git status` 确认无并发提交活动，转换脚本写进 temp/ 并在验证后删除；用 `:is()` 紧凑形式（+228 行而非 +583 行）。
- 新增 UI 功能后：`vite build` → `deploy-test-workspace.mjs --workspace D:/1STUDY/SIYUAN` → 提示重载，三步一起做，别只 build。
- **@container 转换不能只改共享 css**：组件 `<style>` 块里的 `@container` 也要一起换，否则强制模式在宽面板上不生效。脚本要扫所有 `.svelte` + `.css`，跳过 `@container (max-height)`。阈值→层级的映射：包含 tier 当且仅当 `tier.min < threshold`（tier 区间与 (0, threshold] 有交集）。
- tier.min 边界注意：threshold=760 → narrow+tiny（不含 medium，760 < 760 假）；threshold=430 → tiny only（430 < 430 假）。

## 题库工作台：强制移动模式底栏消失修复

- 现象：用户在宽桌面面板上点了快速按钮切到"移动端 UI"，标题栏、tabs、卡片都变成移动样式，但底部快捷条（workspace-quick-bar）+ FAB 消失，工作区下半部分空白。
- 根因：上一轮只转了 `question-bank.css`（共享 CSS），没碰组件 `<style>` 块里的 `@container` 规则。`WorkspaceQuickBar.svelte` / `WorkspaceFab.svelte` / `AnswerModeSwitcher.svelte` / `PracticeLauncher.svelte` / `PracticeQuestionContent.svelte` / `PracticeScanSummary.svelte` / `PracticeScopeTree.svelte` / `QuestionBankSettings.svelte` / `question-bank-panel.css` / `PracticeCompletion.svelte` 仍用 `@container (max-width: Npx)`，实际容器宽度是桌面宽（>760）→ 容器查询失败 → `display: none` 生效。
- 修复：写第二个一次性脚本 `temp/convert-component-container-queries.mjs`，扫整个 `src/lets-question-bank/` 的 .svelte + .css，跳过 node_modules/__screenshots__/dist/.vite/.tmp；用同样的 tier 规则转换所有 `@container (max-width: Npx)` 块，保留 `@container (max-height)` 不动；保留选择器内层缩进。10 个文件被改，0 个 @container max-width 残留。
- 验证：svelte-check 0 error（128 warning 全是既存 unused CSS）；plugin.test + tab-contract 通过；vite build OK；`cp dist/index.js D:/1STUDY/SIYUAN/data/plugins/siyuan-damophus/index.js` 部署；部署副本 index.css 含 293 处 `data-ui-width`（v1 只有 1 处）。用户需 Ctrl+R 重载。

## 题单：支持主键/绑定列 + 按块 ID / 数据库 ID 定位数据库

- `playlist-resolve.ts`：
  - `readPointRowTargets` 支持 `type === "block"` 的列（主键/绑定列）：选中后把每行绑定的块（`value.block.id`）直接作为扫描目标，放进 `directBlockIds`（已是真实块 ID，无需 item→block 映射）；该行没绑定块时记 `unboundPrimaryKey` → 报 `unbound-row`。relation 列逻辑不变。
  - `loadAttributeViewMeta` 给 keys 增加 `isPrimary`（第一个 block 类型列）。
  - 新增 `resolveAttributeViewRef(client, input)`：17 位 ID 先按块查 `SELECT id, ial FROM blocks WHERE id IN ('x')`，用 `/custom-avs="([^"]*)"/` 取 av id（`custom-avs` 可逗号分隔多个，取第一个 `getAttributeView` 成功的），hPath 走 `/api/filetree/getHPathByID`；块路径失败再按 av id 直接 `getAttributeView`。
- `controller.ts` 新增 `resolvePlaylistDatabaseRef`；`QuestionBankUiController` 接口同步。
- `PracticePlaylistManager.svelte`：可选字段从 `type === "relation"` 放宽为 `relation || block`，chip 上标注「主键/绑定 / 关联」；搜索时先试 `resolvePlaylistDatabaseRef`，命中直接 `chooseDatabase` 并跳过关键词搜索（用 `directHit` 标志，避免 finally 把 `searched` 置 true 导致误显示"未找到数据库"）。
- i18n：`playlist.relationKeys/relationKeysHint/noRelationKeys` 换成 `sourceKeys/sourceKeysHint/noSourceKeys`，新增 `keyPrimary/keyRelation`，`searchPlaceholder` 改为支持粘贴 ID（en+zhCN 同步）。
- MockKernelClient 增加 `blockIals`（SQL 请求含 `ial` 时返回该列）与 `blockPaths`（`/api/filetree/getHPathByID`）。
- 验证：题单单测 18 passed（新增主键列扫描、粘贴块 ID / 数据库 ID 两例）；svelte-check 0 error；已 build + deploy 到 `D:\1STUDY\SIYUAN`，部署包含 `custom-avs` 与新 i18n 键。browser 测试环境仍起不来（safe-delete shim + `127.0.0.1 listen EACCES`），新增的「粘贴 ID」browser 用例待环境恢复后跑。

## 题单：预览提速 + 行/列/块→题来源 trace + 名称默认库名

- `playlist-resolve.ts`：
  - `PlaylistRowReport` 重塑为 `{rowItemId, title, totalQuestions, columns[]}`；每列 `{keyId, keyName, kind, blocks[], unboundCount}`；每块 `{blockId, questionCount, questionIds[], unbound?}`（主键列未绑定行也进 blocks，带 unbound 标记）。旧 `targetCount/questionCount` 删除。
  - `PlaylistUnresolved` 加可选 `rowItemId/keyId`；`convergePlaylistTargets` 的 addUnresolved 从 rowTarget 继承上下文（dedupe key 也含 row+key）。
  - `readPointRowTargets` 改为按列记录（`directBlockIdsByKey/targetItemIdsByKey` + keys 元数据 + primaryKeyId）。
  - 新增 `resolvePlaylistQuestions(deps, playlist, {hydrate})`：`hydrate:false` 跳过题目预载（preview 专用，只出 ids/counts/trace）。`loadDocumentBundle`/`listDocumentTreeIds` 改 `Promise.all` 并行。
- `controller.ts` 新增 `previewPlaylist`（hydrate:false）；开练路径 `resolvePlaylist` 保持全量 hydrate。
- `PracticePlaylistManager.svelte`：preview 走 `previewPlaylist`；`chooseDatabase` 后 name 为空则默认数据库名；每行 `<details>` 折叠，展开显示列（名 + 主键/关联）→ 块 ID + 题数 + 前 8 个 questionId（+N 折叠）；unresolved 每条标注「行标题 · 列名 · 原因」，限 30 条。i18n 新增 `playlist.unboundTargets`。
- 测试：题单单测 19 passed（rows 断言全部按新形状重写，新增 hydrate:false 用例断言 hydrateQuestionSources 零调用）；browser fixtures（question-bank.browser.fixtures.ts + PracticePlaylistManager.browser.test.ts）同步新形状 + previewPlaylist mock + 默认名/trace 断言。browser 环境仍 EACCES 起不来。svelte-check 0 error。已 build + deploy。
- 坑：hydration 别在 preview 路径做——它是唯一串行大头的剩余来源；trace 数据要一次解析内顺手收集（converge 的 unresolved 用 rowTarget 继承上下文），别事后反查。
