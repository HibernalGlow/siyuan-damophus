# 2026-08-31

## 题库 Dock 移动端重设计（lets-question-bank）

针对移动端 Dock（~390px）拥挤、空间浪费问题做了两轮重设计，全部走容器查询（`@container ≤760/≤620/≤430`），桌面端不受影响：

- `question-bank.css`：移动端顶栏 tabs 改分段控件（surface 底 + 圆角 12px + 主色 14% 激活态，去下划线）；`.workspace-toolbar .document-row` 改合并式圆角工具条（输入框无边框融入 + 38px 图标按钮，扫描按钮加 `.scan-button` class 做主色高亮）；删掉旧的 44px 大方块按钮规则。
- `AnswerModeSwitcher.svelte`：≤430 恢复图标+文字，全宽分段控件。
- `PracticeLauncher.svelte`：统计带加图标（ListChecks/Check/Circle/X/RotateCcw/Bookmark），≤620 单行 6 列、≤430 隐藏 Again/Hard 子文本；出题/选项顺序 ≤430 两栏并排一行（不再堆两行），分段控件化且恢复文字；legend/label 加主色小图标（Target/ListOrdered/Shuffle/SlidersHorizontal）；开始按钮居中 + 主色投影；选择器 42px 触控高。
- `PracticeConditionEditor.svelte`：≤700 圆角 10px、行高 52px。
- `QuestionBankWorkspace.svelte`：扫描按钮加 `class="scan-button"`。

验证：`pnpm build` 通过（26s）；预览稿 `output/mobile-dock-redesign-preview.html` + 截图 png（Playwright 390px）。

## 题库 Dock 桌面端同步 + 移动端残留修复（v3）

同步桌面端设计语言并解决返工反馈，继续用容器查询：`question-bank.css` 桌面顶栏改成 slim 分段控件（~48px，surface 胶囊背景）；`.practice-launcher-body` 桌面端去掉右侧 268px 按钮列，改底部通栏操作条（左提示 / 右按钮）；`.workspace-maintenance` 桌面端改双栏网格，移动端仍单列。`PracticeLauncher.svelte` 修复了移动端「开始练习」被挤出首屏的问题：原本 `position: sticky; bottom:8px` 在最后一个子元素上无效，改为在 ≤700 容器里用 `order: -1` 把操作条提到表单之上；同时把「未完成的练习」从全宽色带改成紧凑卡片 + 列表高度限制。

扫描摘要：`PracticeScanSummary.svelte` 将 `.summary-grid` 基准列数改为 `repeat(6, minmax(0,1fr))` 并加 `overflow:hidden; border-radius`，根治列数≠条目数导致的灰色空白格；把「考点关系同步」改为分段控件（与 tabs/模式切换设计语言一致）；「展开详情」改成带边框圆角 chip（`.scan-details` 用外层 div 包装，避免 `Collapsible.Root` 不转发 class 导致 CSS 未命中）。

验证：
- `pnpm build` 通过（40.6s / 25.9s / 25.97s 三次）。
- `pnpm check`：修复前我引入 7 条 `Unused CSS selector` 警告；修复后剩余 11 个错误均为改动前既有问题，分布在我未修改的文件（`tab-replacement.test.ts`、`pause-on-blur.browser.test.ts`、`Statistics.svelte`、`question-bank.browser.fixtures.ts`、`lets-mobile-breadcrumb/index.ts`、`lets-flashcard/plugin.test.ts`、`flashcard/category-module.ts`）。
- Playwright 双端截图：`output/dock-desktop-preview.png`（1200px）、`output/dock-mobile-preview.png`（390px）。移动端 390px 列中「开始练习」按钮 top≈343px，位于首屏内。

### 坑点
- WorkBuddy 沙箱有 safe-delete 计数（每 turn 50 个文件），`pnpm build` 清 dist 会触发 SAFE_DELETE_BULK_CONFIRM_REQUIRED；换 turn 或首次构建可过。
- Tailwind v4 工具类在 @layer 内，组件里非 layered 自定义 CSS 可直接覆盖，无需 !important。
- `position: sticky` 对最后一个子元素无效：它的可粘范围被父元素底边限制，父容器底边就是其自然位置。
- `bits-ui` 的 `Collapsible.Root` 是逻辑/context 组件，不会渲染 DOM，class 不会被转发；需要样式作用域时应包一层 div。
